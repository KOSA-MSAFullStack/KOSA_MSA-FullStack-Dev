<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jino.DAO.BoardDAO">
	<select id="list" resultType="com.jino.DTO.BoardDTO">
		select * from BOARD
		order by idx desc
	</select>
	<select id="getBoard" resultType="com.jino.DTO.BoardDTO">
		select * from BOARD
		where idx= #{idx}
	</select>
	
	<insert id="create">
		INSERT INTO BOARD (TITLE, CONTENTS, AUTHOR)
		values( #{title}, #{contents}, #{author} )
	</insert>
	<update id="update">
		update BOARD
		set title = #{title}
			,contents = #{contents}
			,AUTHOR = #{author}
		    , CREATED_AT = sysdate
		where idx=#{idx}
	</update>
	<delete id="delete">
		delete from BOARD
		where idx=#{idx}
	</delete>
</mapper>

