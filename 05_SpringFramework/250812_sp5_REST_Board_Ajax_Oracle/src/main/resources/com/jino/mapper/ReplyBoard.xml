<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.jino.mapper.ReplyBoard">
  
  <select id="list" resultType="com.jino.domain.ReplyVO">
  select * from tbl_reply2 
  where bno = #{bno}
  order by rno desc
  </select>
 
 <insert id="create">
 insert into tbl_reply2( bno, replytext, replyer)
 values(  #{bno}, #{replytext}, #{replyer})  
 </insert>

 <update id="update">
 update tbl_reply2 
 set replytext = #{replytext}, updatedate = sysdate
 where rno=#{rno}
 </update>
 
 <delete id="delete">
 delete from tbl_reply2 
 where rno=#{rno}
 </delete>

</mapper>
  